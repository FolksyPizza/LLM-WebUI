# Current Backlog

This file tracks non-blocking improvements to consider next.

## UX / Product
1. Add inline (non-alert) validation messages in admin forms.
2. Add search/filter inside admin `View Chats` modal.
3. Add optional provider-specific token cap hints for compatibility.

## Streaming / Chat
1. Add native streaming handlers for Anthropic and Google providers (currently full-response with streamed chunk fallback in UI layer).
2. Add explicit “jump to latest” affordance when auto-scroll is paused.

## Docs
1. Add end-user quickstart screenshot walkthrough.
2. Add provider-specific examples (OpenAI/vLLM, Anthropic, Google) in a dedicated docs page.
